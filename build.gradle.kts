// Shared build logic lives in buildSrc as the `jbplugins.intellij-plugin` conventions
// plugin, so this file stays empty of plugin configuration. Versions are in
// gradle/libs.versions.toml; repo-wide policy is in gradle.properties.
